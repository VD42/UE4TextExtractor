version: 2.1
orbs:
  win: circleci/windows@2.4.1
jobs:
  build:
    executor:
      name: win/default
      shell: cmd.exe
    steps:
      - checkout
      - run:
          name: "Build"
          command: "msbuild UE4TextExtractor.sln -p:Configuration=Release"
